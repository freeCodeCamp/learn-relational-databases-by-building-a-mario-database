/*const assert = require('assert');
const { getLastLog } = require('./utils');

describe('You', () => {
  let lastLog;
  before(async () => {
    lastLog = await getLastLog(true);
  });

  it('should view "character_id" column for all rows from "characters"', () => {
    const selectAll = /statement:selectcharacter_idfromcharacters;/i;
    assert(selectAll.test(lastLog));
  });
});*/
